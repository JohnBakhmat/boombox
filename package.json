{
  "name": "boombox",
  "private": true,
  "workspaces": [
    "web",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "db:migrate": "bun --filter @boombox/backend db:migrate",
    "db:gen": "bun --filter @boombox/backend db:gen"
  },
  "devDependencies": {
    "oxfmt": "^0.16.0"
  }
}
